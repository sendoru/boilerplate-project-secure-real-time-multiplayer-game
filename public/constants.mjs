const WINDOW_WIDTH = 640;
const WINDOW_HEIGHT = 480;

const TOP_BAR_HEIGHT = 0;
const GAP = 0;

const X_MAX = WINDOW_WIDTH - TOP_BAR_HEIGHT - 2 * GAP;
const Y_MAX = WINDOW_HEIGHT - 2 * GAP;

export { WINDOW_WIDTH, WINDOW_HEIGHT, TOP_BAR_HEIGHT, GAP, X_MAX, Y_MAX };
